// Placeholder entry point required by the MCPB manifest schema. The actual
// server is launched via mcp_config (npx mcp-remote → hub.arcade.dev);
// this file is never executed by Claude Desktop.
process.exit(0);
